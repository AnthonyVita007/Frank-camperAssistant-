[flask]
# ATTENZIONE: cambia questa chiave in produzione
SECRET_KEY = frank-dev-secret-change-me
DEBUG = true
HOST = 0.0.0.0
PORT = 5000

[llm_intent_detection]
# Enable LLM-based intent recognition (requires local LLM server)
enabled = true
# Confidence thresholds for hybrid decision making
confidence_threshold_high = 0.8
confidence_threshold_low = 0.5
# Maximum time to wait for LLM response (seconds)
timeout = 5.0
# Cache settings for performance
cache_max_size = 100
cache_ttl = 300